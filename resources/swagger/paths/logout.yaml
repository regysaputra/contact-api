delete:
  tags:
    - user
  description: Logout current user
  security:
    - bearerAuth: []
  responses:
    '200':
      description: Success logout user
      content:
        application/json:
          schema:
            type: object
            properties:
              data:
                type: boolean
              errors:
                type: object
